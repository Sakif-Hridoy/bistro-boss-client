import React from 'react';
import PropTypes from 'prop-types';

const Login = props => {
    return (
        <div>
            <h2>Please Login</h2>
        </div>
    );
};

Login.propTypes = {
    
};

export default Login;