import React from 'react';
import PropTypes from 'prop-types';

const Secret = props => {
    return (
        <div>
            <h2>Secret Things</h2>
        </div>
    );
};

Secret.propTypes = {
    
};

export default Secret;